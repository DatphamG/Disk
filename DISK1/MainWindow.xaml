<Window x:Class="DISK1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DISK1"
        mc:Ignorable="d"
        Title="Disk Space Analyzer" 
        Height="700" Width="1100"
        MinHeight="600" MinWidth="900"
        Background="#1E1E1E" 
        WindowStartupLocation="CenterScreen"
        WindowStyle="SingleBorderWindow"
        ResizeMode="CanResize">

    <Window.Resources>
       
        <Style x:Key="DarkButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#0D7377"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                BorderThickness="0">
                            <Border.Effect>
                                <DropShadowEffect Color="#14FFEC" 
                                                  BlurRadius="12" 
                                                  ShadowDepth="0" 
                                                  Opacity="0.25"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#14FFEC"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#0A5F62"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryDarkButtonStyle" TargetType="Button" BasedOn="{StaticResource DarkButtonStyle}">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3E3E42"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DarkTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderBrush" Value="#3E3E42"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                          Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

       
        <Style x:Key="DarkCardStyle" TargetType="Border">
            <Setter Property="Background" Value="#252526"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" 
                                      BlurRadius="25" 
                                      ShadowDepth="0" 
                                      Opacity="0.3"/>
                </Setter.Value>
            </Setter>
        </Style>

     
        <Style x:Key="DarkTreeViewStyle" TargetType="TreeView">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>

       
        <Style x:Key="DarkProgressBarStyle" TargetType="ProgressBar">
            <Setter Property="Height" Value="8"/>
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="#14FFEC"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Grid>
                            <Border Background="{TemplateBinding Background}"
                                    CornerRadius="4"/>
                            <Border CornerRadius="4">
                                <Grid>
                                    <Rectangle x:Name="PART_Track" />
                                    <Rectangle x:Name="PART_Indicator"
                                               Fill="{TemplateBinding Foreground}"
                                               HorizontalAlignment="Left">
                                        <Rectangle.Effect>
                                            <DropShadowEffect Color="#14FFEC" 
                                                              BlurRadius="8" 
                                                              ShadowDepth="0" 
                                                              Opacity="0.6"/>
                                        </Rectangle.Effect>
                                    </Rectangle>
                                </Grid>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Background="#1E1E1E">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        
        <Border Grid.Row="0" 
                Background="#252526" 
                Height="50"
                BorderBrush="#3E3E42"
                BorderThickness="0,0,0,1">
            <Grid>
                <TextBlock Text="💾 Disk Space Analyzer" 
                           FontSize="16"
                           FontWeight="SemiBold"
                           Foreground="#E0E0E0"
                           VerticalAlignment="Center"
                           Margin="24,0,0,0"/>
            </Grid>
        </Border>

       
        <Grid Grid.Row="1" Margin="24,20,24,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            
            <Border Grid.Row="0" 
                    Style="{StaticResource DarkCardStyle}"
                    Margin="0,0,0,15"
                    Padding="16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBox x:Name="txtPath" 
                             Grid.Column="0"
                             Style="{StaticResource DarkTextBoxStyle}"
                             IsReadOnly="True"
                             Text="No folder selected"/>

                    <Button x:Name="btnBrowse" 
                            Grid.Column="1"
                            Content="📁 Select Folder"
                            Style="{StaticResource SecondaryDarkButtonStyle}"
                            Width="140"
                            Margin="12,0,0,0"
                            Click="BtnBrowse_Click"/>

                    <Button x:Name="btnScan" 
                            Grid.Column="2"
                            Content="🔍 Scan"
                            Style="{StaticResource DarkButtonStyle}"
                            Width="100"
                            Margin="12,0,0,0"
                            Click="BtnScan_Click"/>
                   
                </Grid>
            </Border>

            
            <Border Grid.Row="1"
                    x:Name="progressPanel"
                    Style="{StaticResource DarkCardStyle}"
                    Margin="0,0,0,15"
                    Padding="16"
                    Visibility="Collapsed"
                    >
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    
                    <Grid Grid.Row="0" Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock x:Name="txtProgressInfo" 
                                   Grid.Column="0"
                                   Text="Scanning files..." 
                                   FontSize="12"
                                   Foreground="#E0E0E0"
                                   VerticalAlignment="Center"/>

                        <TextBlock x:Name="txtProgressPercent" 
                                   Grid.Column="1"
                                   Text="0%" 
                                   FontSize="12"
                                   FontWeight="SemiBold"
                                   Foreground="#14FFEC"
                                   VerticalAlignment="Center"/>
                    </Grid>

                   
                    <ProgressBar Grid.Row="1"
                                 x:Name="progressBar"
                                 Style="{StaticResource DarkProgressBarStyle}"
                                 Minimum="0"
                                 Maximum="100"
                                 Value="0"/>
                </Grid>
            </Border>

           
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="3*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" 
                        Style="{StaticResource DarkCardStyle}"
                        Margin="0,0,12,0">
                    <Grid>
                        <TextBlock Text="📊 Chart Area (Coming Soon)" 
                                   Foreground="#3E3E42"
                                   FontSize="20"
                                   FontWeight="Bold"
                                   HorizontalAlignment="Center" 
                                   VerticalAlignment="Center"
                                   TextWrapping="Wrap"
                                   TextAlignment="Center"/>
                    </Grid>
                </Border>

                <Border Grid.Column="1" 
                        Style="{StaticResource DarkCardStyle}"
                        Margin="12,0,0,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>


                        <Border Grid.Row="0" 
                                Background="#2D2D30"
                                CornerRadius="8"
                                Padding="12,8"
                                Margin="0,0,0,12">
                            <TextBlock Text="📂 File Explorer" 
                                       FontSize="14"
                                       FontWeight="SemiBold"
                                       Foreground="#14FFEC"/>
                        </Border>

                       
                        <Border Grid.Row="1" 
                                Background="#1E1E1E"
                                CornerRadius="8"
                                Padding="8"
                                BorderBrush="#3E3E42"
                                BorderThickness="1">
                            <ScrollViewer VerticalScrollBarVisibility="Auto"
                                          HorizontalScrollBarVisibility="Auto">
                                <TreeView x:Name="treeViewFiles" 
                                          Style="{StaticResource DarkTreeViewStyle}">
                                    <TreeView.ItemTemplate>
                                        <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                                            <Grid Margin="0,2">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>

                                                <!-- File Info -->
                                                <TextBlock Grid.Column="0" 
                                                           Text="{Binding DisplayName}" 
                                                           Padding="4"
                                                           FontSize="13"
                                                           Foreground="#E0E0E0"
                                                           VerticalAlignment="Center">
                                                    <TextBlock.ToolTip>
                                                        <ToolTip Background="#2D2D30" Foreground="#E0E0E0" BorderBrush="#3E3E42">
                                                            <StackPanel Margin="5">
                                                                <TextBlock Text="{Binding Name}" FontWeight="Bold" Foreground="#14FFEC"/>
                                                                <TextBlock Text="{Binding FullPath}" FontStyle="Italic" Margin="0,5,0,0"/>
                                                                <TextBlock Text="{Binding SizeFormatted, StringFormat='Size: {0}'}"/>
                                                                <TextBlock Text="{Binding LastModified, StringFormat='Modified: {0:g}'}"/>
                                                            </StackPanel>
                                                        </ToolTip>
                                                    </TextBlock.ToolTip>
                                                </TextBlock>

                                                <!-- Hover Actions -->
                                                <StackPanel Grid.Column="1" 
                                                            Orientation="Horizontal" 
                                                            Margin="12,0,0,0"
                                                            VerticalAlignment="Center"
                                                            Visibility="Visible">
                                                    
                                                    <!-- Open Button (Folder Icon) -->
                                                    <Button ToolTip="Open Item"
                                                            Margin="0,0,6,0"
                                                            Background="Transparent"
                                                            BorderThickness="0"
                                                            Cursor="Hand"
                                                            Click="BtnOpen_Click"
                                                            Padding="4">
                                                        <Button.Template>
                                                            <ControlTemplate TargetType="Button">
                                                                <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                                                                    <Path Data="M10,4H4C2.89,4 2,4.89 2,6V18A2,2 0 0,0 4,20H20A2,2 0 0,0 22,18V8C22,6.89 21.1,6 20,6H12L10,4Z" 
                                                                          Fill="#14FFEC" 
                                                                          Stretch="Uniform" 
                                                                          Width="16" 
                                                                          Height="16"/>
                                                                </Border>
                                                                <ControlTemplate.Triggers>
                                                                    <Trigger Property="IsMouseOver" Value="True">
                                                                        <Setter Property="Background" Value="#2D2D30"/>
                                                                    </Trigger>
                                                                </ControlTemplate.Triggers>
                                                            </ControlTemplate>
                                                        </Button.Template>
                                                    </Button>

                                                    <!-- Delete Button (X Icon) -->
                                                    <Button ToolTip="Delete Item"
                                                            Background="Transparent"
                                                            BorderThickness="0"
                                                            Cursor="Hand"
                                                            Click="BtnDelete_Click"
                                                            Padding="4">
                                                        <Button.Template>
                                                            <ControlTemplate TargetType="Button">
                                                                <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                                                                    <Path Data="M19,6.41L17.59,5L12,10.59L6.41,5L5,6.41L10.59,12L5,17.59L6.41,19L12,13.41L17.59,19L19,17.59L13.41,12L19,6.41Z" 
                                                                          Fill="#FF4444" 
                                                                          Stretch="Uniform" 
                                                                          Width="16" 
                                                                          Height="16"/>
                                                                </Border>
                                                                <ControlTemplate.Triggers>
                                                                    <Trigger Property="IsMouseOver" Value="True">
                                                                        <Setter Property="Background" Value="#2D2D30"/>
                                                                    </Trigger>
                                                                </ControlTemplate.Triggers>
                                                            </ControlTemplate>
                                                        </Button.Template>
                                                    </Button>
                                                </StackPanel>
                                            </Grid>
                                        </HierarchicalDataTemplate>
                                    </TreeView.ItemTemplate>
                                </TreeView>
                            </ScrollViewer>
                        </Border>
                    </Grid>
                </Border>
            </Grid>
        </Grid>

       
        <Border Grid.Row="2" 
                Background="#252526"
                Padding="24,12"
                BorderBrush="#3E3E42"
                BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock x:Name="txtStatus" 
                           Grid.Column="0"
                           Text="Ready to analyze disk space" 
                           FontSize="12"
                           Foreground="#888888"
                           VerticalAlignment="Center"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>